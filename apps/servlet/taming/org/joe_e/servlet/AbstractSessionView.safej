# Manually modified safej file for Joe-e mail.
class("org.joe_e.servlet.AbstractSessionView",
  static(constructor(allow, "AbstractSessionView()", comment("allowed for Joe-E mail. We need this so that Joe-E servlets can extend it and use the default constructor"))),
  instance(method(suppress, "fillHttpSession(HttpSession)", comment("default deny")),
    method(suppress, "fillSessionView(HttpSession)", comment("default deny"))))
