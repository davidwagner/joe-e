# Manually edited but has not been audited yet. 
# Allowing things that we need for webmail
class("java.util.HashMap",
  static(constructor(allow, "HashMap()", comment("allowed for webmail")),
    constructor(suppress, "HashMap(Map<? extends K,? extends V>)", comment("default deny")),
    constructor(suppress, "HashMap(int)", comment("default deny")),
    constructor(suppress, "HashMap(int, float)", comment("default deny"))),
  instance(method(suppress, "clear()", comment("default deny")),
    method(suppress, "clone()", comment("default deny")),
    method(allow, "containsKey(Object)", comment("allowed for webmail")),
    method(suppress, "containsValue(Object)", comment("default deny")),
    method(suppress, "entrySet()", comment("default deny")),
    method(allow, "get(Object)", comment("allowed for webmail")),
    method(suppress, "isEmpty()", comment("default deny")),
    method(suppress, "keySet()", comment("default deny")),
    method(allow, "put(K, V)", comment("allowed for webmail")),
    method(suppress, "putAll(Map<? extends K,? extends V>)", comment("default deny")),
    method(suppress, "remove(Object)", comment("default deny")),
    method(suppress, "size()", comment("default deny")),
    method(suppress, "values()", comment("default deny"))))
