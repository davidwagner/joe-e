# Manually modified safej file for Joe-e mail.
class("org.joe_e.servlet.Dispatcher",
  static(constructor(suppress, "Dispatcher()", comment("default deny")),
  method(allow, "logMsg(String)", comment("allowed"))))
